server:
  port: 8060
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringcloudConfig/
          search-paths: respo
      label: master
  application:
    name: config-server
